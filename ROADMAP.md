# ROADMAP.md — Intralys CRM

> Source de vérité unique pour le statut + prochaines étapes.
> Dernière mise à jour : 2026-05-15.

## Vision

CRM tout-en-un universel pour PMEs francophones (courtiers, dentistes, plombiers, coachs, cleaning, agences). Pas niche immobilier. Différenciateurs : compliance Loi 25/CASL native, AI FR québécois, packs industrie 1-clic, prix abordable vs GHL/HubSpot.

Marché cible : 200k PMEs QC, 800k Canada, 5M+ francophones global.

## Stack figée (cf. `docs/archive/` historique)

- **Backend** : TypeScript + Cloudflare Workers + D1 (SQLite edge)
- **Storage** : Cloudflare R2
- **Realtime** : Durable Objects (WebChat)
- **Frontend** : React 19 + Vite + Tailwind v4 + TanStack Router
- **AI** : Claude Haiku 4.5 (Anthropic)
- **Email** : Resend (avec mocks dev)
- **SMS** : Twilio (avec mocks dev)
- **Mobile futur** : PWA puis Capacitor V1 puis React Native V2 si traction
- **Hosting** : Cloudflare Pages

Voir `README-DEV.md` pour le setup local sans clés externes.

## Sprints accomplis ✅

| # | Sprint | Effort | Status |
|---|---|---|---|
| 0 | P0+P1+P2+P3 (foundations) | ~30j | ✅ Commité |
| Design 1+2 | Refonte UI palette Intralys cyan/orange | ~10j | ✅ Commité |
| Consolidation + Conversations | Mocks + tests + webchat + FB DM | ~17j | ✅ Commité |
| 4 | Pivot générique + Pipeline + Workflows | ~17j | ✅ Commité (75%, 8 triggers/actions backlog) |
| 5 | Vertical Calendar + Tasks | ~8j | ✅ Commité |
| 6 | Différenciateurs Intralys (D1-D7) | ~12j | ✅ Commité |
| 7 | Templates + Forms + AI killer | ~12j | ✅ Commité |
| 8 | Settings + Reports + Agency admin | ~17j | ✅ Commité |
| 9 | Mobile responsive + PWA | ~6j | ✅ Commité |
| 10 | Polish + Beta privée + Launch | ~10j | ✅ Commité |
| 11 | Capacitor V1 — App native iOS/Android | ~15j | ✅ Commité |
| 12 | Production Hardening & Core UX | ~8j | ✅ Commité |
| 13 | API publique + Webhooks OUT + Zapier | ~10j | ✅ Commité |
| 13.5 | Hotfix audit (tsconfig worker.ts + webhooks robustes + role api) | ~2j | ✅ Commité |
| 14 | GHL Migration importer (CSV + OAuth hybride) | ~15j | ✅ Commité |
| 14.5 | Hotfix audit GHL Migration (OAuth CSRF/AES, tags/CF, idempotence réelle) | ~1j | ✅ Commité |
| 16 | Design System Migration — Legacy Modal/Input éliminés | ~1j | ✅ Commité |
| 16.5 | Polish visuel + tokens CSS dans toutes les pages | ~0.5j | ✅ Commité |
| 17 | Crédibilité avant beta — fix P0/P1 UX-friction (résidus + WS status + notes dedup + mobile responsive) | ~2j | 🟡 Code-complete (build/tests à valider) |
| 18 | Slide-over panels + perf optimiste (différenciation UX vs GHL) | ~5j | 🟡 Code-complete (build/tests à valider) |
| 19 | AI Sparkles inline + Command palette intent engine + optimistic msg send | ~5j | 🟡 Code-complete (build/tests à valider) |
| 20 | AI summarize convo + suggest next action + keyboard shortcuts + retry failed | ~4j | 🟡 Code-complete (build/tests à valider) |
| 21 | Quick-Add FAB + density modes + AI batch summarize + smart lists pinned sidebar | ~5j | 🟡 Code-complete (build/tests à valider) |
| 22 | TaskPanel (stack 3-deep) + Activity feed temps réel + Lead timeline unifiée | ~5j | 🟡 Code-complete (build/tests à valider) |
| 26 | Design depth scope réduit — **6 sub-tâches** (orchestration 3 Managers × 2, skip Chaman) : **Inbox bubbles premium** (MessageBubble asymmetric iMessage-style + gradient sent + read receipts color-coded jaune chaud / MessageAttachment images thumb+zoom + files chip + audio waveform stub / MessageReactions hover-only 6 presets + DropdownMenu 18) + **Forms aesthetics** (Input/Select/Textarea props label/helper/error/success/leftSlot/rightSlot/containerClassName + shake 320ms spring + check overshoot 280ms + warning pulse 1.4s + 3 formulaires wirés Login/ChangePassword/Leads modal) + **Multi-step wizards** primitive `<Wizard>` (steps progress chip + connectors gradient + slide direction-aware 280ms + persistKey localStorage) + TeamSettings wizard 3 steps (Email/Rôles/Message) + BrandingSettings wizard 4 steps (Logo/Couleurs 5 palettes prédéfinies/Société/Pages publiques) | ~0.5j | 🟡 Code-complete (build/tests à valider) |
| 25 | Design depth pixel-perfect — **12 vagues atomiques** (orchestration Chaman + 3 Managers : Système visuel fondations / Sensoriel + feedback / Polish surfaces dynamiques) : Typography scale 7 niveaux + Color depth (18 brand tints `--brand-50..900` + 6 shadow brand-tinted scale + 4 surface elevations) + Primitive `<Icon>` wrapper Lucide (stroke 1.75 signature Linear, size enum xs/sm/md/lg/xl mapping 12/14/16/20/24) + refresh 5 primitives sur surfaces (Card/Modal/PageHero/DropdownMenu/SlidePanel) + **Sensorial system** (3 nouveaux modules : useSound 7 sons procéduraux Web Audio toggle/success/error/notif/send "plink" Superhuman/celebrate/tick + useHaptic 5 intensités + lib/sensorial singleton AudioContext lazy + câblage 8 endpoints + Settings panel "Feedback sensoriel" 12 preview buttons) + 3 hover preview hooks (TaskHoverPreview/ConversationHoverPreview/AppointmentHoverPreview wirés Tasks+Inbox+Calendar) + EmptyStateIllustration 6 SVG inline contextuels (leads/tasks/pipeline/inbox/calendar/reports gradient cyan→orange) migration 6 pages + .bg-noise texture subtile + View Transitions enrichies par catégorie (workspace slide 280ms / inbox push 240ms / settings fade 200ms / builder zoom 240ms) + AppLayout route-category dispatch + Pipeline drag premium (setDragImage ghost clone tilt 2° + drop pulse) + Calendar polish (working hours 06/12/18 highlights + today pulse vue Mois + live line setInterval 60s) | ~1.5j | 🟡 Code-complete (build/tests à valider) |
| 24 | Polish UX/UI design pur — **12 vagues atomiques** (orchestration Chaman + 3 Managers : Data UX / Comms UX / Polish UX) : 6 nouvelles primitives (BulkActionBar, FilterChip+AppliedFiltersBar, Combobox, Coachmark, AutosaveIndicator) + 1 hook `useAutosave` + 3 pages erreurs premium (NotFound 404 SVG + OfflineFallback + ErrorBoundary class component wrap RouterProvider) + InteractiveTour 5 steps (sidebar/search/fab/notifs/density) + bulk-select Tasks+Inbox (Linear hover-checkbox + Gmail shift-click), filters chips dismissable Leads+Tasks+Pipeline+Inbox (élimine `<select>` natifs), notif dropdown grouping chrono + filter chips Toutes/Non lues/Mentions + actions inline reveal-on-hover, Toast variant hero gradient brand bounce + `confettiBurstSubtle`/`confettiBurstHero`, CommandPalette Favoris (cmd+D) + Saved Searches sections, AutosaveIndicator 5 états sticky top-right wired 4 Settings forms, Button ripple DOM-only + Tag pulse + Card variant interactive | ~2j | 🟡 Code-complete (build/tests à valider) |
| 23 | Visual lift dramatique vs "Windows XP" — **47 vagues** | ~16j | 🟡 Code-complete (build/tests à valider) |
| 27 | Design depth — Tables premium (frozen header/sticky col/expand row/score tooltips) + PipelineSettings 3 tabs inline + Tasks table mode (3ᵉ vue) + 9 erreurs TS corrigées | ~1j | 🟡 Code-complete |
| 28 | Performance + Mobile + AI — Pull-to-refresh hook + AI Insight Cards Dashboard (3 variantes contextuelles hot-lead/stuck-deal/week-wins) + CSS animations | ~0.5j | 🟡 Code-complete |
| 29 | Dashboard Presets + UX — 3 presets Manager/Agent/Admin (widget visibility one-click) + Shift+R refresh shortcut + loadData refactor | ~0.5j | 🟡 Code-complete |
| 41 | **Pages secondaires Stripe — Inbox + Calendar refonte** (Sprint 1/3 trilogie GIGA, 3 Managers parallèles M1+M2 puis M3 séquentiel, ~30 min wall-clock total) — Couverture Stripe paradigm sur 2 pages cœur encore plates post-Sprint 38 RESET. **M1 Inbox refonte Stripe-grade** : nouvelle primitive `<MessageBubble>` à `src/components/ui/MessageBubble.tsx` (231L) namespacée `.msg-bubble-ui-*` pour zéro collision avec composant existant `components/Inbox/MessageBubble.tsx` — props `{ direction: 'in'|'out', content, timestamp, status?, reactions?, attachments?, replyTo?, avatarSrc?, avatarName? }` asymétrique sober bg `--surface` (in) + `--primary-soft` (out) radius 12 sauf coin émetteur 4 max-w 70 + avatar 32 + timestamp `t-meta` + status icon Check/CheckCheck/Eye via Lucide 12 + reactions chips bg-subtle border gray-200 (reactedByMe = primary-soft) + hover reactions emoji bar 6 emojis (👍 ❤️ 😂 😮 😢 🎉) circles 28 ; ConversationsList.tsx refonte (332L) header/search/tabs/channel-chips/row/empty-state all Stripe-clean — gradients brand cyan/orange WIPED → primary purple #635BFF + shadows `--shadow-xs/sm/md` Stripe noir 5-10% + filter chips is-active = primary-soft bg + rgba(99,91,255,.30) border + primary text + active row conv = bg primary-soft + inset 3px 0 0 0 primary ; Inbox.tsx refonte (754L) Summarize button + AI summary card + Empty state central (icon 64px gray-300 cercle bg-subtle + h3 17px + p body muted + CTA primary "Plus + Nouveau message" wired setIsComposingNew(true)) + Empty state liste mini-version 56px icon FR québécois ("Tes échanges...") + CTA "Mail + Nouvelle conversation" ; MessageComposer.tsx hint chip "Tapez / pour insérer une variable" t-meta 11px muted bottom-right rgba blanc 65% backdrop-blur + kbd `/` chip gray + fade-out :focus-within. Reactions/QuickReplies wirage Promise déjà fonctionnel (MessageReactions persistence localStorage optimistic+rollback / MessageComposer.applyQuickReply FIFO 3 recordReply onSendResolved) — vérifié intact. CSS append `/* ── Sprint 41 M1 — Inbox refonte Stripe (2026-05-15) ── */` **+795L**. **M2 Calendar refonte Stripe** : Calendar.tsx refonte complète (1056L vs ~1020L) drop PageHero/KpiStrip/BottomSheet/EmptyStateIllustration/APPOINTMENT_TYPE_COLORS/ICONS/gradients brand/halos colorés/glow shadows/animations dramatic ; Toolbar sticky top nav prev/next icon-only `.cal-nav-btn` 28×28 sober + chip Aujourd'hui + view switcher pill row gray-100 bg avec is-active surface-blanc shadow-xs + Button primary "Nouvel événement" + date range t-h2 20px font-600 capitalize ; Grid Stripe cells border-gray-200 1px hover bg-hover today col bg primary-soft 50% off-hours/on-hours identiques (subtle) ; Event chips surface BLANC + border gray-200 + border-left 3px color-coded via `--event-color` variant brand/info/success/warning/danger/accent/neutral title 12/600 time 10 tabular muted hover shadow-sm border-gray-300 drag opacity 0.5 resize ring 2px primary (sober pas glow) ; Resize handles top/bottom 4px invisible idle opacity 0.7 hover/focus bg primary ns-resize cursor (hidden pointer:coarse mobile) ; Today live-line horizontal 2px primary col jour courant + dot 8px primary pulse 2s box-shadow ring expand (`prefers-reduced-motion` respected) ; Mini-cal sidebar 7×6 grid 28px round-sm font 12 tabular today=inset ring primary selected=bg primary white has-events=dot 4px primary 70% bottom other-month=gray-400 nav prev/next mois sober ; Filtres calendars row sober switch+dot 10+name hover bg-hover focus-visible outline primary ; SlidePanel droite size=sm détail event remplace Modal+BottomSheet dual header title auto tags type+status rows datetime/lieu/lead/calendar/notes border-bottom subtil footer 4 actions ghost (Confirmer/Rappel/Dupliquer/Annuler-danger) ; Empty state center overlay `.cal-empty-overlay` backdrop subtle 88% week/day/agenda no-events icon CalendarDays 48px gray-300 h3 17 desc 13 muted CTA primary ; Agenda rows compactes button-like date-block 44px (day big bold + month tiny uppercase) title-row tag type + tag status statusIcon meta clock+calendar+location ; Variants AppointmentType → Tag (meeting→brand call→info visit→success signing→accent followup→warning) AppointmentStatus → success/danger/warning/info. CSS append `/* ── Sprint 41 M2 — Calendar refonte Stripe (2026-05-15) ── */` **+898L**. **M3 Polish + micro-détails** : nouveau hook `src/hooks/useShortcuts.ts` (163L) window-level + ref pattern (un seul addEventListener mount, fresh closures via `bindingsRef`) skip inputs SAUF si modifier accompagne + `defaultPrevented` check anti-double-fire avec Textarea onKeyDown send-on-Enter + cross-platform `Cmd+X` ≡ `Ctrl+X` via alias + `e.repeat` skip anti-spam ; Inbox shortcuts j/k nav, r focus composer (querySelector `.composer-wrap textarea`), e archive + toast undo CTA 5s, Escape cascade (bulk → composing → deselect), Cmd+Enter send (+127L) ; Calendar shortcuts t/n/w/d/m + ←/→ via `navigate(±1)` adapte viewMode Escape ferme detail panel/modal (+116L) ; Toasts wirés non-invasivement après setConversations/setAppointments/await jamais en remplacement — bulk actions Inbox affichent count CTAs undo 5s archive (Inbox) cancel (Calendar) échecs CTA "Réessayer" ; Skeleton Calendar week/day refonte 7 colonnes (ou 1 day) gutter hours 4 events placeholders positionnés déterministes 9h-17h (`(colIdx + i) % 2`) shimmer border-left 3px gray-300 + Agenda skeleton date+main matchant row réelle ; A11y aria-label `inbox-summarize-btn` + aria-hidden icons focus-visible CSS systémique nav buttons/pills/events/agenda rows/filter rows (outline 2px primary outline-offset 2px) announceSR polite nouveau message inbound + changement conv ouverte + count unread changes + view switch Calendar ; Reduced-motion bloc dédié désactive shimmer skeleton live-dot pulse composer chips transitions drag-resize. CSS append `/* ── Sprint 41 M3 — Polish + micro-détails (2026-05-15) ── */` **+127L** (4973→5100). **Total Sprint 41 : ~1820L CSS append-only** + 8 fichiers TSX/TS (MessageBubble.tsx nouveau, useShortcuts.ts nouveau, Inbox.tsx + Calendar.tsx + ConversationsList.tsx + MessageComposer.tsx + ui/index.ts + index.css). Préservations 100% : drag-resize Sprint 31 (HOUR_PX=64/SNAP_MIN=15/MIN_DURATION_MIN/ResizingState/startResize/onMove/onUp verbatim) + slash-vars MessageComposer + reactions emoji bar + quickReplies per-lead FIFO 3 + Sprint 40 Tag statusIcon + long-press mobile + bulk-select Gmail-pattern (refactor Esc → useShortcuts comportement identique) + AppointmentHoverWrap Sprint 25. API publique 100% back-compat. Aucun orb / gradient brand massif / glow brand-tinted ajouté (variant accent = brand-orange UNIQUEMENT bordure 3px event chip, jamais surface). FR québécois informel partout. | ~0.5j | 🟡 Code-complete (build/tests à valider) |
| 40 | **Details Stripe-grade** post-Sprint 39 (3 Managers parallèles, ~9 min wall-clock) — Sprint 40 continue le bump sophistication SANS retour DRAMATIC. **M1 KPI deep + Tag statusIcon** : Sparkline primitive enrichie props `strokeWidth?:number` (défaut 2) + `terminalDotStripe?:boolean` (défaut false) 100% back-compat ; Sparkline KPI 32→**44px** + stroke 2→**2.5** + dot terminal Stripe (r=3.5 white ring 1.5px sans glow halo) + color-coded variant via `.dashboard-kpi-card--{variant} .dashboard-kpi-sparkline-dot-terminal` ; **Hover reveal secondary metric** `.dashboard-kpi-secondary` calculé depuis sparkData slice halves → "X cette sem · Y prev sem." text 11px muted tabular-nums + transition opacity + max-height + padding-top 200ms + reduced-motion respect ; **Tag `statusIcon?: boolean \| LucideIcon`** prop (défaut false back-compat 100%) auto-mapping success→CheckCircle2 / warning→Clock / danger→XCircle / info+brand→Info / accent→Sparkles / neutral+default→aucune + override custom via `statusIcon={MyLucideIcon}` + priorité rendu `statusIcon > leftIcon > dot` + icône 11px stroke 2.25 (bumped vs 1.75 baseline) via `.tag-status-icon` ; **Wirages 4 pages** : Dashboard table contacts mobile (xs) + desktop (sm) `statusIcon` (remplace dot), Leads card status pill + modal Notes statut row `STATUS_TO_TAG_VARIANT` mapping + `statusIcon` (remplace color raw), Tasks status column `statusIcon` (suppr emoji TASK_STATUS_ICONS), Pipeline stage badge variant dérivé `stage.probability` (≥90 success / =0 danger / ≥50 warning / sinon info) + `statusIcon`. **M2 Charts + Sparklines inline** : BarChart migré `<ComposedChart>` (recharts) + `<Line>` overlay `type="monotone"` stroke primary strokeWidth 2 **strokeOpacity 0.35** dot=false isAnimationActive false (reduced-motion respect) + `<LabelList>` custom `PeakLabel` value 11px gray-500 weight 600 tabular-nums au-dessus uniquement bars `count >= maxCount*0.8` (peaks) + tooltip Sprint 39 `BarChartTooltip` + axes Stripe préservés + pattern IIFE scope maxCount/peakThreshold ; Helper `MiniSparkline({ data, color })` SVG inline 60×16 polyline normalisé min-max stroke 1.5 round caps/joins + helper `sparkTrendForSource(source)` 7 points **déterministe** (sin wave + drift seed par charCode source pas mock random) + wired `.dashboard-source-row` à droite avant value chip avec sourceColor Sprint 39 ; Helper `ActivityTypeDot({ activityType })` 6×6 round mapping `created→success`, `email_sent\|sms_sent\|tag_added\|assigned→primary`, `status_change→warning`, `note_added\|tag_removed→muted`, `deal_value_changed→success` + inséré `.dashboard-activity-row` avant Avatar margin-top 14px aligné centre Avatar sm (28px). **M3 Live indicators + Hover details** : Sidebar `.sidebar-nav-item-badge-wrap` + `.sidebar-nav-item-live-dot .pulse-dot` 6px à droite count>0 color-coded via `--dot-color` inline (`leadsNew→primary`, `tasksTodo→warning`, `notifsUnread→success`) ; Dashboard hero state `lastFetchAt` + setter `loadData finally` + useEffect setInterval 60s force re-render + bouton `.dashboard-hero-updated-at` sous sub-text avec icon RefreshCw 12px label "Mis à jour il y a X min" / "à l'instant" minutesAgo≤0 + click `void loadData()` + icon rotate 180° au hover ; Derniers contacts `<tr.dashboard-contact-row>` cell Activité wrap time + `.dashboard-contact-row-actions` 3 boutons (Mail mailto / Phone tel: / ChevronRight openPanel) + reveal opacity 0→1 + translateX -4→0 + `pointer-events: none` hidden + `:focus-within` accessible ; Activity feed `.dashboard-activity-row-open` "Ouvrir →" subtle primary 11px reveal au hover row. ~500L CSS append-only Sprint 40 sections 40-1A/1B/2A/2B/3A/3B. Pas DRAMATIC. API 100% back-compat. 7 fichiers TSX modifiés (Tag.tsx, Sparkline.tsx, Dashboard.tsx, Leads.tsx, Tasks.tsx, Pipeline.tsx, Sidebar.tsx). | ~0.5j | 🟡 Code-complete |
| 39 | **Personality Bump Stripe-PLUS** post-Sprint 38 RESET (3 Managers parallèles, ~7 min) — User feedback : "plus beau mais simple plat". Bump sophistication SANS retour dramatic (pas d'orbs, pas de gradient brand massif, pas de glow). **M1 Typography + KPI** : `.t-display 32→40px 700 -0.025em`, `.t-h1 24→28px 700 -0.02em`, `.t-h2 20→22px 700`, `.t-h3 16→17px`, `.t-caption 500→600` (plus contraste), `.t-meta 600→700 0.06→0.08em`, ajout `.t-body-strong 14/600` + `.section-header` accent border-left 3px primary padding-left 12px ; KPI cards refonte StatCardMockup : icon chip 40×40 droite color-coded soft bg + icon `--{variant}` 20px, value 32px/700/tabular-nums/-0.02em, delta chip pill soft bg + icon TrendingUp/Down 14px + "vs période précédente" label muted, **border-top accent 2px** ::before color-coded (primary Total / success Pipeline / warning Taux / brand-cyan signature Intralys Revenu) + hover lift -1px + shadow-sm + border-strong. **M2 Charts + Activity** : BarChart `barCategoryGap=20%` + `radius={[4,4,0,0]}` rounded top + fill primary solid + axes Stripe `axisLine=false tickLine=false` ticks 11px gray-500 + `CartesianGrid strokeDasharray="3 3"` horizontal only + custom `<BarChartTooltip>` Card sober (white+border+shadow-md) date fr-CA + header inline total 20px tabular + chip delta success/danger vs demi-période + height 220→240 ; Donut innerRadius 68→72 outerRadius 100→112 strokeWidth 2→3 + centre value 28px tabular + `t-meta` "Total leads" + legend structurée `.dashboard-donut-legend-item/dot/label/count/pct` hover bg-hover + Cell hover `activeDonutIdx` state scale 1.04 + drop-shadow 180ms ; Top sources bars 6→8px **color-coded par source** (Site web purple primary / Facebook `#1877F2` / Google `#EA4335` / Référence success / Phone warning / Instagram `#E4405F` / Direct text-muted) + row hover bg-hover + chevron reveal ; Section header `::before 3px × 20px primary radius 2px` + border-bottom + h3 17px tracking-tight appliqué Activité récente + Top sources ; Activity rows Avatar sm + content (font-semibold name + italic action + primary entity) + time chip pill `.dashboard-activity-time` bg-subtle radius-sm 11px muted + CTA "Voir toute l'activité" arrow translateX(2px) hover. **M3 Cards hover + Sidebar** : `.card-interactive-bump` opt-in hover **-2px lift** + `--shadow-md` + border gray-300 (vs -1px shadow-sm Phase 2 baseline) active -1px feedback tactile + `.row-premium cursor: pointer` signal interactivité ; Top contacts items hover `translateY(-1px)` + ring 3px primary-soft sur Avatar enfant (`> :first-child`) + helpers `.dashboard-top-contact-name/-count` ; "+ Ajouter" card bump hover `translateY(-1px)` + chip `bg primary solid + white icon + rotate(90°)` + label passe `var(--primary)` ; Sidebar WHITE Stripe confirmé + override `.sidebar-nav-item.is-active` border-left 2→3px primary paddings recompensés + variant `.sidebar-dark` fourni bonus (bg primary solid + text white + border white) + **badges helper `.sidebar-nav-item-badge`** inactive bg-gray-200 text-gray-700 hover bg-gray-300 active **bg-primary text-white** `font-feature: 'tnum'` Stripe-grade + Logo Intralys 32→**36px** font 14→15px wordmark "Intralys" passe `text-primary` gris → `var(--primary)` purple Stripe + font-bold. ~400L CSS append-only Sprint 39 sections 39-1A/1B/2A/2B/3A/3B. Pas de retour DRAMATIC. | ~0.5j | 🟡 Code-complete |
| 38 | ⚠️ **SPRINT RESET — TURNAROUND** (Chaman audit profond + 7 agents Phase 1-2-3) — User dixit "tu fait pas vraiment du vraie travail de logiciel et app moderne" + "Claude empile code sur code". Décision validée user : RESET COMPLET design system Sprint 23-37 + reconstruction **Stripe Dashboard paradigm**. **Phase 1 Wipe CSS** : `src/index.css` 8265L → **2497L (-70%, -5768L)** + tokens Stripe (`--gray-50..900`, `--primary #635BFF`, `--shadow-xs/sm/md/overlay` noir subtle, `--border` rgba 50/50/93 .10) + brand Intralys cyan/orange préservés signature uniquement (logo/favicon/print/2-3 CTAs) + AGENTS.md section Sprint 38 RESET ajoutée tête fichier + memory `feedback_design_dramatique` CADUQUE. WIPED : `--brand-50..900`/`--accent-*` tints scale, `--shadow-brand-*` cyan-tinted scale, ~25 keyframes décoratifs (hero-orb-float/hot-lead-pulse/dashboard-*-pulse/cmd-fav-bounce/etc.), dark mode oklch, view transitions named per category Sprint 25, .bg-noise/.bg-orbs-canvas neutralisés display:none, custom scrollbar gradient cyan→orange. **Phase 2 Refonte 15 primitives core Stripe-clean** (3 agents parallèles) : M1 Forms (Button/Input/Textarea/Select/Tag) + M2 Overlays (Modal/DropdownMenu/Tooltip/Avatar/Badge) + M3 Layout (Card/KpiStrip/EmptyState/Skeleton/Toast) — ~65 inline styles décoratifs supprimés (gradients brand cyan→orange, orbs, halos, glow brand, shadow tinted bi-color), API publique 100% préservée avec aliases compat (`premium→primary`, `destructive→danger`, `default→neutral`), variants additifs non-breaking. **Phase 3 Refonte 7 pages cœur** (3 agents parallèles) : M1 Dashboard+AppLayout+Sidebar (3 orbs hero supprimés + `t-display gradient brand` → `t-h1` 24px gray-900 + KPI value 64px gradient → 28px tabular-nums + charts SVG `<linearGradient>+<filter>` supprimés + donut gradient stops → solid + sidebar dark navy → white Stripe avec border-right + active state `bg-primary-soft + border-left 2px primary` signature + logo Intralys cyan→orange PRÉSERVÉ) + M2 Leads+LeadDetail+LeadHoverPreview (PageHero supprimé → `<header>` Stripe + map fallback gradient/orb wipés + Mapbox popup HTML titre gradient → `#111827` sober + hero double gradient + orb 260px → bg-surface + shadow-xs + ViewTransitionName avatar-{id} Sprint 35 PRÉSERVÉ + 4× gradient brand clip-text → text-primary) + M3 Pipeline+Tasks (PageHero supprimé + kanban columns gradient + glassmorphism + box-shadow brand → white card + border + radius + PipelineLeadCard hot-lead gradient + badge HOT gradient + Avatar ring="hot" → white card + border-left 3px primary + subtask checkbox gradient `#009DDB→#D96E27` glow → `bg-primary` solid). ~76 inline styles décoratifs supprimés. **Total : ~140 inline styles WIPED**. **Logique métier 100% préservée** : dnd-kit Pipeline + Calendar drag-resize, ghost-card setDragImage clone, card resize 3 modes localStorage, URL params filters useRouterState TanStack + replaceState + popstate sync (Sprint 31), AI features 5 insights + 3 sorts + 3 drafts + 6 score signals + 12 fuzzy search fields, bulk select Linear+Gmail patterns, view modes 4 pages, view transitions named avatar-{id} Sprint 35, mobile gestures SwipeAction + useLongPress + BottomSheet + usePullToRefresh + useEdgeSwipe + QuickAddFab fan-out, sensorial useSound 7 sons + useHaptic 5 patterns, A11y focus-visible + 16 WCAG fixes + LiveRegion SR, Capacitor lifecycle + i18n maison + Web Vitals + E2E Playwright + ShareButton + PDF exports, Wizard 3 wizards Team/Branding/ApiWebhooks, Coachmark+InteractiveTour onboarding, AutosaveIndicator + useAutosave, Reactions+QuickReplies persistence Promise-pattern, Tables premium 10 pages frozen+expand row, Network status banner. Compat back-compat préservé : 23 classes legacy noms intacts visuel Stripe sober + aliases CSS `--brand-primary→--primary`, `--surface-0..3→--bg-canvas/surface`, `--shadow-brand-*→--shadow-xs/sm/md/overlay`. Wall-clock total : **~27 min parallèle** (vs estimation Chaman ~2h30 → 5× plus rapide). | ~1.5j | 🟡 Code-complete (build/tests à valider) — TURNAROUND RESET |
| 36 | **Dashboard DRAMATIC OVERHAUL** — **6 sub-tâches SLIM** (3 Managers × 2) déclenché par screenshot user "le design il reste encore beaucoup je trouve regarde toi même" + audit confirme dashboard pas reçu wave 1-2-3 dramaticité Sprint 23 (opacités 5-8% invisibles viole AGENTS.md règle d'or 12-30%+) : **Hero "Bonjour" refonte** titre `t-h2` 28px → `t-display` clamp(40-56px) + gradient brand shift 6s background-size 220% + 2 orbs invisibles → **3 orbs dramatic 22%/38%/42%** + boutons plats → "Exporter" `action-chip--accent` orange premium + fond white pur → `surface-2` + border bottom gradient brand subtil + shadow aucune → brand 20% + wave 👋 statique → pulse animé 7.2s + **KPI cards `.dashboard-kpi-premium`** orb 32%→34% cyan ou 30% orange color-matched + pulse 4s + shadow noir → **brand-tinted bi-color cyan+orange** (18-32% repos / 32-42% hover) + big number standard → `text-display` clamp(48-64px) gradient brand + hover translateY-3px + scale icon 1.08 + glow boost + focus-visible ring + orb tint auto (sparkColor brand/success→cyan, warning/danger→orange) + **AI Insight cards opacité 5%→22%** (variants hot-lead/stuck-deal/week-wins/dormant/velocity background gradient 0.08/0.03→0.22/0.10) + borders 0.20→0.40-0.44 + **orb décoratif 130px** opacité 0.28 blur 40px color-matched + sparkle chip 28×28 gradient brand 22%+orange 16% border 38% pulse 2.4s + CTA native button classe premium + **Charts dramatic** BarChart audit confirme bar-acquisition+bar-glow déjà actifs Sprint 15 + bonus halo cyan box-shadow container + **Donut segments épaissis** innerRadius 62→68 outerRadius 90→104 container 200→220 + stroke white 2.5px + drop-shadow brand 0.28 + chart-glow-pulse 4s + bordure-bottom signature gradient cyan→orange 1px + **Top contacts row** 6 cards plates → 4 `<Avatar size="lg" ring="active" status animate>` + hover translateY(-3px) scale(1.04) + glow ring brand cyan 65% + tooltip "Voir profil" + lead count `.t-caption text-gradient-brand` + **"+ Ajouter" card** border dashed grise → **gradient brand cyan→orange 18-26%** + chip 40×40 blanc icon Plus brand rotate 90° au hover + label gradient + flex gap-6 overflow-x-auto scroll mobile + **Top sources bars 7px** wrappées `.dashboard-source-bar-container` inset shadow brand 14% + edge highlight blanc 60% + fill gradient cyan→cyan-light→orange + glow 10px brand + 6px orange + inset highlight + **shimmer `dashboard-source-bar-shimmer-final` 2.8s linear infinite** + row hover bandeau gauche 2px gradient cyan→orange + bg brand 10% + translateX(2px) + chevron reveal + Badge `<Tag variant="brand" size="xs" solid>` au lieu texte plat + stagger entrance 60ms + **Derniers contacts table-premium** wrapper card avec orb décoratif top-right 18% blur 40px + Avatar ring color-coded (won→active vert / qualified→hot brand-orange) + `<Tag dot variant>` success/warning/accent/brand/neutral + `<Tag dot pulse>` si qualified + Source `<Tag neutral>` emoji + Valeur `<Tag success>` + **score bar 6px gradient color-coded** success/warning/danger + shimmer + slide-in 800ms stagger 80ms + `.row-premium` partout (bandeau gauche 3px gradient cyan→orange + glow hover). **+826L CSS** appendées sur `index.css`. Zone unique modifiée Dashboard.tsx (3 zones distinctes hero+KPI / charts+AI / right+tables) + AiInsightCard.tsx | ~0.5j | 🟡 Code-complete (build/tests à valider) |
| 35 | Production-ready + polish final — **12 sub-tâches FULL** (18 entités : Chaman + 3 Managers × 4 — Performance & Quality / i18n + Mobile native / Docs + Animations + Cleanup) : **Perf foundations** `index.html` lang=fr-CA + format-detection telephone=no + dns-prefetch API + fetchpriority=high script entry + `vite.config.ts` target=es2020 + reportCompressedSize=true + cssCodeSplit=true + manualChunks raffinés (vendor-dnd ~30KB / vendor-radix ~80KB / vendor-xyflow ~100KB) → **Dashboard initial bundle -120-160KB critical path** + **TanStack route prefetch** `defaultPreload:'intent' + defaultPreloadStaleTime:30_000` → nav sub-100ms (vs 250-400ms cold) + **`src/lib/webVitals.ts` NOUVEAU** zero-dep PerformanceObserver natif LCP/CLS/INP/TTFB/FCP avec seuils Google officiels + `initWebVitals(onMetric)` + `reportToBackend(m, endpoint?)` sendBeacon-first SSR-safe gates + **`tests/e2e/smoke.spec.ts` NOUVEAU** 5 flows critiques (Login→Dashboard greeting / Leads→LeadPanel slide-over / Pipeline column / Tasks FAB création rapide / Inbox composer ou empty) + playwright.config.ts project `smoke` séparé visual + **`src/lib/i18n.ts` NOUVEAU** zero-dep ~95L `t(key, vars?)` fallback chain fr-CA → en → raw key + `{{var}}` interpolation + getLocale/setLocale/availableLocales + storage localStorage.intralys_locale + tsconfig.json resolveJsonModule:true + `src/i18n/fr-CA.json` + `src/i18n/en.json` 30 strings critiques (5 API errors + 14 Auth + 5 Errors 404 + 5 Offline + 2 Share) wired src/lib/api.ts 3 errors + **Swipe-to-delete Inbox** ConversationsList wrap SwipeAction props onArchive/onDelete actions chips 40px archive neutre + delete rouge #E5484D threshold 80px cohérent Tasks + **`src/components/ui/ShareButton.tsx` NOUVEAU** variants icon/labeled feature-detect navigator.share + fallback clipboard.writeText + toast t('share.copied') + haptic light + sound success + AbortError silent wired LeadDetail toolbar à côté Export PDF + **Capacitor native polish** `ios.scheme/server.iosScheme/androidScheme:'https'` aligne cookies Same-Site backend CF + `StatusBar.overlaysWebView:false` + `SplashScreen.androidScaleType:'CENTER_CROP'` + `initCapacitorLifecycle()` App resume/pause/backButton Android stub via @capacitor/app dynamic import + safe-area utilities `.safe-bottom/.safe-top` opt-in + body padding auto mode PWA standalone via @media (display-mode:standalone) + @supports (padding:env(...)) + **`docs/DOCS-PRIMITIVES.md` NOUVEAU** ~330L denses catalog 49 primitives (48 + ShareButton) 7 familles (Layout / Form / Feedback / Navigation / Data display / Mobile / Advanced) + index alphabétique en tête + chaque primitive 1 ligne purpose + signature TS condensée + exemple minimal + Sprint d'origine + **View Transitions named** AvatarProps `style?: CSSProperties` rétrocompat + Leads.tsx 2 Avatar (cartes 932 + table 1347) + LeadDetail hero 499 `viewTransitionName: 'avatar-' + lead.id` + CSS `::view-transition-group(avatar-*)` durée 320ms ease brand + safety net animation-duration:1ms reduce-motion (Chrome 111+/Edge/Safari 18+ interpolation auto / Firefox/Safari<18 fallback plat silencieux) + **CSS dedup audit annotatif** 5 doublons TODO-S36 identifiés (shimmer-h hard / 5 keyframes float* / chip-btn+action-chip focus override / 6 shimmer* / 13 pulse-* variants ~80L économie) PAS de delete/rewrite + **Archive cleanup 11 plans Sprint** (16/16.5/17/18/19/20/21/22/23/27/30 PLAN.md) → docs/archive/ après vérif no-overwrite + racine 7 .md restants (AGENTS/BETA-CODES/HANDOFF/LAUNCH-CHECKLIST/README-DEV/ROADMAP/STRATEGY) | ~1.2j | 🟡 Code-complete (build/tests à valider) |
| 34 | Polish final pré-prod — **6 sub-tâches SLIM** (3 Managers × 2, skip Chaman) : **PDF exports premium consolidé** `src/lib/pdfExport.ts` helper `triggerPdfExport(mode)` toggle body class + window.print() + cleanup afterprint + ~330L CSS `@page A4 12mm` + page numbering bottom-center brand cyan + 5 classes covers communes (`.pdf-cover-accent-bar/logo/tagline/title/subtitle/meta`) + 3 modes (invoice/report/lead-sheet) avec règles spécifiques + override `body.pdf-mode-active` masque sidebar/header/composer/skip-link/toast + Loi 25 card masqué `data-lead-sheet-internal` + 3 PDF templates wirés (Invoices toolbar + Reports primary + LeadDetail icon Download) + **Brand assets fix critique** favicon SVG existant **violet `#863bff` incohérent brand → remplacé** par gradient cyan→orange 64×64 + lettre "I" blanche + glow + OG image créée `public/og-image.svg` 1200×630 wordmark + 3 chips différenciateurs (Loi 25 native / AI FR québécois / Packs industrie) + Meta tags OG totalement absents `index.html` → ajoutés complets (OG + Twitter summary_large_image + locale fr_CA) + manifest.webmanifest favicon.svg en premier + dédoublement icon-192/512 purpose any+maskable PWA best-practice (theme_color #009DDB + bg_color #FAFBFC déjà conformes) + **`useNetworkStatus` hook** SSR-safe + **`<NetworkStatusBanner />` primitive** 3 phases internes (idle→offline→back-online→idle 3s) position fixed top:56px z-50 pill compacte + variant offline gradient orange→rouge 22%/20% border rouge 40% + back-online gradient success→cyan auto-dismiss 3s + haptic+sound Sprint 25 endpoints + skip 1er render hasMountedRef + role status aria-live polite + **Toast queue management** limite 5 toasts max sweet-spot UX (Gmail 5 / Linear 3 / Vercel 5) + tri par sévérité error>warning>success>info récents intra-type + overflow chip "+N autres" gradient brand pulse 2.4s click→panel compact dot color-coded + clear-all ghost danger discret rgba 8% bg + **`<AppBootScreen tagline?>` Suspense fallback** fullscreen orbs + halo pulse 2s + progress slide 1.4s + `<AiLoadingShimmer text? size?>` 3 dots gradient brand staggered 200ms variants sm/md/lg intégré MessageComposer popover drafts ~350ms stub Claude Haiku + **`<LiveRegion>` SR primitive** auto-clear 5s + `lib/announce.tsx` singleton `announceSR(message, politeness?='polite')` dedup 500ms par région + `<LiveRegionPortal />` mounted AppLayout + wired Toast (error→assertive autres polite) + BulkActionBar (changement selectedCount via useRef anti re-render) + AutosaveIndicator (saved polite / error assertive) + re-announce trick `setX('')+queueMicrotask(setX(message))` force commit 2× pour mutation textContent même message identique | ~0.5j | 🟡 Code-complete (build/tests à valider) |
| 33 | Final completion + polish — **6 sub-tâches SLIM** (3 Managers × 2, skip Chaman) : **Icon migration FINAL** ~20 fichiers UI primitives (Toast/Button/Input/Textarea/Select/Modal/DropdownMenu/SlidePanel/Coachmark/AutosaveIndicator/BottomSheet/Wizard/AiSparkles/SmartBanner/PhoneLink/DateRangePicker/ScopePicker/InstallPrompt/KeyboardShortcutsModal/FeedbackWidget/AiInsightCard) → **cumul 88 fichiers** (100% primitives UI + ~95% codebase) avec collision AiSparkles résolue alias UIcon + rename local ActionIcon + **WCAG focus-visible 8 fixes** (.filter-chip ring brand 3px / --active override gradient / .bulk-action-bar button manquait / .scope-chip + wildcard / .autosave-pill button retry / .chip-btn outline-offset 2px fonds custom orbs+glass / .action-chip même fix / .ai-draft-option bump 3→4px contrast 3:1 sur gradient brand / .skip-link inner ring blanc multi-fond) + bloc prefers-reduced-motion + **`lib/reactions.ts`** Promise pattern ready-for-backend (`toggleReaction(msgId, emoji, userId): Promise<Reaction[]>` schéma localStorage `{[msgId]: {[emoji]: userIds[]}}` tri count desc + emoji alpha + optimistic UI useRef rollback futur-proof + userIds stockés pour tooltip avatars futurs) wired MessageBubble messageId prop chip click direct évite double-roundtrip + **`lib/quickReplies.ts`** per-lead (`recordReply` trim + skip vide + skip >280 chars + dedup bring-to-top + cap FIFO 3) wired MessageComposer onSendResolved async + row chips above textarea CSS `.quick-reply-row` scroll-x mask fade + `.quick-reply-chip` pill gradient surface/brand 20% + hover gradient cyan-orange + lift + ellipsis + prefix ↩ chip-style + **Pipeline cards drag-resize hauteur** 3 modes snap (compact 80 / normal 140 / expanded 240px) handle 28×6px bottom gradient brand cyan→orange + pattern Calendar S31 (mousedown capture + window mousemove pickHeightModeForDelta 60px/step + haptic medium snap) + triple anti-conflit DnD (stopProp + preventDefault + draggable=false) + compact mask-image fade bottom 70→35% + is-resizing ring brand 2px + 6px halo + persist localStorage intralys_pipeline_card_heights + mobile pointer:coarse display:none + **final visual audit 7 fixes** opacités 5%→12%+ (ConversationPanel/AiNextActionCard idle+active/Dashboard/Leads/NewConversationPane bg-white plat→card-premium/OnboardingWizard shadow-sm→shadow-brand-xs hover lift) + CSS safety net `bg-white.border-subtle:not(.card-premium):not(...)` force shadow-brand-xs minimum rattrape surfaces flat futures | ~0.5j | 🟡 Code-complete (build/tests à valider) |
| 32 | Search depth + AI deepen + Tables completion — **6 sub-tâches SLIM** (3 Managers × 2, skip Chaman) : Smart search Leads `fuzzyScoreMulti` sur 12 champs pondérés (name 1.00 / email 0.92 / phone 0.88 / tags 0.86 / company 0.82 / city 0.74 / notes 0.70 / message 0.66 / address 0.60 / budget/timeline/property_type) + helper `highlightSearch` avec `<mark className="search-hit-mark">` gradient cyan→orange + pop-280ms + **WCAG AA 8 fixes systémiques** (`--text-muted` #8A93A4→#6B7488 ratio 3.18:1→4.86:1 PASS AA / `--text-faint` nouvelle var sub-AA / `.t-caption` bind explicite / `.t-label-form` #009DDB→#007AAF 3.55:1→5.10:1 PASS / `.t-micro` 8:1 PASS AAA) règles scopées `:root:not([data-theme="dark"])` preserve dark theme latent + `lib/aiDrafts.ts` NOUVEAU `generateDrafts(lastMessage, leadCtx?)` 3 tones (short cyan/detailed gradient brand/awaiting warning) avec heuristiques détection `?` + prénom + topic stop-words FR + MessageComposer trigger Sparkles stacké toolbar gauche popover 360px click-outside/Escape + Inbox passe `lastInboundMessage` lookup reverse direction + `lib/leadScoreExplain.ts` NOUVEAU `explainScore(lead): ScoreSignal[]` 6 signaux (Activité récente / Engagement msg+tasks / Étape pipeline / Valeur deal / Source / Tags chauds conditionnel hot/qualified/priority/vip) + LeadDetail Card "Pourquoi ce score ?" sous Score Gauge avec chip "Signal AI" + bar color-coded vert/rouge proportionnelle weight + chip ±X tabular-nums + 4 dernières tables premium (Templates frozen col icône+nom expand preview 280c+usage+last_used / Workflows frozen icône trigger+statut expand trigger+actions_count+last_run / Reviews conversion list Card → table-premium frozen auteur+5★ gradient brand expand commentaire+source+lead+zone réponse inline / Trash frozen Avatar+nom expand deleted_at+jours purge colorisé danger/warning) pattern Sprint 27 réutilisé **zéro nouveau CSS** + `<React.Fragment key>` expand rows + Icon migration **22 fichiers** () ~125 icônes wrappées → **cumul 74 fichiers ~79% Lucide migré** + conflit M2 LeadDetail section score signals `strokeWidth={2.5}` intentionnels préservés | ~0.5j | 🟡 Code-complete (build/tests à valider) |
| 31 | Wiring + completion — **6 sub-tâches SLIM** (3 Managers × 2, skip Chaman) : URL params CmdPalette consommés `useRouterState` TanStack + replaceState + popstate sync bidirectionnel — Leads (`?status/source/client/scoreMin`), Pipeline (`?stage/owner` fallback graceful), Tasks (`?status/priority/assignee/overdue=true` synthétique) + chips AppliedFiltersBar étendus + PtR Inbox via refacto `ConversationsList` props minimal (`onRefresh?: () => Promise<void>` + `usePullToRefresh` conditionnel) avec endpoints sensoriels Sprint 25 (haptic medium + sound success) + Properties grid Cards → table-premium frozen col 1 (thumbnail + nom) + Invoices `.row-premium` → full table-premium **2 colonnes frozen** (numéro + client) avec breakdown taxes **TPS+TVQ 14.975%** (compliance QC native) + Documents frozen col + expand workflow timeline Envoyé/Vu/Signé + `lib/aiSort.ts` 3 heuristiques (contact_priority `score×0.6 + min(30,daysIdle×2.1) + log10(deal+1)×6` / conversion_likelihood stageMomentum / dormant_value filter daysIdle≥14) + bouton "Trier intelligemment" toolbar Leads + DropdownMenu + CmdPalette section "AI Sort: ✨" deep-links + CSS `.ai-sort-button` + sparkle 2400ms + Icon migration **31 fichiers** (pages secondaires + hover previews + panels + settings + Inbox + widgets — cumul S25+30+31 ≈ 52 fichiers) avec 3 collisions Icon locale résolues (UIcon alias / rename EventIconCmp + ActionIcon) + Calendar drag-resize events (state machine useRef + global mousemove/mouseup listeners + optimistic UI + rescheduleAppointment persist + snap 15min/min 15min/max 8h + 2 handles 28×8px + outline brand 2px + bi-color shadow + cal-resize-pulse 1.4s + mobile `pointer:coarse` display:none + haptic medium snap/success release + anti-conflit DnD `onPointerDown stopPropagation`) | ~0.5j | 🟡 Code-complete (build/tests à valider) |
| 30 | Finition design depth — **12 sub-tâches FULL** (orchestration Chaman + 3 Managers × 4 : Data UX completion / Inbox + Wizards + AI / Mobile + Icons) : 3 nouvelles primitives (`CellHoverInfo` riche tooltip 280ms + breakdown 7 tones + trend + sparkline, `ScopePicker` grid catégorisée 8 scopes API + 12 events webhook, `PullToRefreshIndicator` factorise spinner brand) + 1 hook `useEdgeSwipe` (mobile back gesture edge 20px threshold 100px + haptic light/medium + auto-disable non-touch) + 2 utilities (`lib/fuzzy.ts` Levenshtein word-boundary bonus, `lib/snippetVars.ts` 8 vars résolution lead context) + Reports tables premium `.table-premium-container` + CellHoverInfo wired 7 cells + CommandPalette fuzzy `fuzzyScoreMulti` + section Filtres `status:`/`source:`/`client:`/`reports:` + `REPORTS_VIEWS` deep-links 12 vues + Reports URL persist `?view=&period=` via replaceState + popstate sync TanStack + Settings nav search global Input premium leftSlot + `<mark>` highlight + MessageComposer slash menu split `/xyz` snippets vs `/var xyz` 8 vars (name/email/phone/deal_value/stage/client_name/score/today) preview live vert/gris italique + chips AlertTriangle vars manquantes + ApiWebhooksSettings Modals → Wizard 2 steps + ScopePicker grid (8 scopes `read:`/`write:` × 4 ressources, 12 events 3×4 catégories + wildcard) + Wizard `embedded` prop (default false préserve Sprint 26) + AiInsightCard 2 nouveaux variants (`dormant-leads-30d` indigo seuil ≥5 leads non-fermés updated_at <30j + `pipeline-velocity-drop` danger drop >30% wins last 7d vs 7-14d) | ~1j | 🟡 Code-complete (build/tests à valider) |

**Total livré : ~239.7 jours de dev.**

## Sprint actuel 🔜

| Sprint | Thème | Durée estimée | Statut |
|---|---|---|---|
| 24 | (optionnel) Lead score gauge + hover preview + Mapbox theme + demo data | ~3j | 🟡 En cours (wave 9 Sprint 23) |


## Sprints restants & Giga-Roadmap 100 Sprints 🔜

> [!TIP]
> Une feuille de route complète d'ingénierie et de parité GoHighLevel (GHL) sur 100 sprints a été formalisée. Elle détaille l'ensemble des migrations SQLite (`seq146` à `seq195`), des endpoints Workers et des structures d'UI.
>
> 📖 Consulter le document de référence : [GIGA-ROADMAP-100-SPRINTS.md](file:///C:/Users/rochdi/.gemini/antigravity-ide/scratch/intralys-dashboard/docs/GIGA-ROADMAP-100-SPRINTS.md)

### V2 Backlog & Prochaines Vagues
- **Vague 6 (S51 - S60)** : Téléphonie & VoIP Avancée (numéros virtuels, IVR visuel, répondeurs, transcriptions Whisper, push mobile FCM/APNS).
- **Vague 7 (S61 - S70)** : Moteur de Funnels, A/B Testing, Multi-entrepôts, tarifs B2B et calcul automatique de taxes locales.
- **Vague 8 (S71 - S80)** : IA & Automatisation cognitive (RAG, bot conversationnel autonome, analyse de sentiments, copilote de vente).
- **Vague 9 (S81 - S90)** : Management multi-tenant (SaaS d'agences, portail en marque blanche, custom domains SSL Cloudflare for SaaS, snapshots).
- **Vague 10 (S91 - S100)** : Haute performance Edge, rate-limiting DO/KV, chiffrement AES-GCM, conformité Loi 25 automatisée et SQLite mobile local.

## Métriques objectif

- **Sprint 10 (Beta)** : 5 clients test gratuits validés
- **3 mois post-Sprint 10** : 20 clients payants à $47-97/mois
- **6 mois** : 100 clients = $5-10k MRR
- **12 mois** : 500 clients = $25-50k MRR → embauche 1er dev

## Docs actifs

- `README.md` — overview projet (auto-généré par Vite)
- `README-DEV.md` — setup local sans clés externes
- `ROADMAP.md` — ce doc
- `STRATEGY.md` — positionnement marché + concurrents
- `docs/ANTIGRAVITY-DEPTH-AUDIT.md` — checklist enrichissement page par page
- `docs/ANTIGRAVITY-GHL-INVENTORY.md` — référence features GHL extrait via API live
- `docs/ANTIGRAVITY-GHL-COMPLETENESS.md` — vue d'ensemble "fermer GHL"
- `docs/archive/` — sprints accomplis + plans historiques
- `docs/design/design-mockup.html` — mockup palette Intralys

## Convention

- 1 sprint à la fois, pas de parallèle
- Auto mode actif (Antigravity exécute, valide par push commit)
- Tests Vitest obligatoires sur features critiques
- Build vert après chaque commit
- Format commits : `feat(scope): ...`, `fix(scope): ...`, `chore(scope): ...`
- Anti-collision : `git log --oneline -10 <fichier>` avant de toucher
